{
  "name": "@timebeat/desktop",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "build": "echo 'Desktop build requires static export, skipping...'",
    "build:release": "tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.5.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.5.0"
  }
}
